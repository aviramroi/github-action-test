name: Shell Command

on: [push]

jobs:
  run-shell-comman:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run : echo "Hello world"
      - name: multiline script
        run: |
          node -v
          npm -v